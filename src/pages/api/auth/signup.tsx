import { NextApiHandler } from "next";

const handler:NextApiHandler=async (req,res)=>{
    if(req.method==="GET"){

        //do sth
    }
    if(req.method==="POST"){

        
    }


}
export default handler;